package com.kmlc.strongo.data.local.ExerciseLoader

import com.kmlc.strongo.R

enum class Mechanics(val resId: Int) {
    COMPOUND(R.string.compound),
    ISOLATION(R.string.isolation),
    PULL(R.string.pull)
}
