package com.kmlc.strongo.data.local.ExerciseLoader

import com.kmlc.strongo.R

enum class BodyRegion(val resId: Int) {
    CORE(R.string.core),
    FULL_BODY(R.string.full_body),
    LOWER_BODY(R.string.lower_body),
    UPPER_BODY(R.string.upper_body)
}
