package com.kmlc.strongo.data.local.ExerciseLoader

import com.kmlc.strongo.R

enum class ForceType(val resId: Int) {
    OTHER(R.string.other),
    PULL(R.string.pull),
    PUSH(R.string.push),
    PUSH_PULL(R.string.push_pull),
    UNSORTED(R.string.unsorted)
}
