package com.kmlc.strongo.data.local.ExerciseLoader

import com.kmlc.strongo.R

enum class ContinuousOrAlternatingLegs(val resId: Int) {
    ALTERNATING(R.string.alternating),
    CONTINUOUS(R.string.continuous)
}
